# GitHub

Human intro for GitHub + `gh` CLI patterns used across repos.

**Agents:** durable notes live in [`gh-workflow-notes.md`](./gh-workflow-notes.md). Do not use this README as operating procedure — root [`../../AGENTS.md`](../../AGENTS.md) High README rule.

Prefer `gh` over raw curl when authenticated. Feature branch → PR → merge for shared remotes. Standards pointer: [`../../docs/standards/github-iac-security.md`](../../docs/standards/github-iac-security.md).
